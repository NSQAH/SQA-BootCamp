{
  "name": "piautomation",
  "version": "1.0.0",
  "description": "testing the super calculator",
  "main": "conf.js",
  "dependencies": {
    "jasmine-data-provider": "^2.2.0",
    "protractor": "7.0.0",
    "protractor-jasmine2-html-reporter": "0.0.7"
  },
  "scripts": {
    "wupdate": "./node_modules/.bin/webdriver-manager update",
    "wstart": "./node_modules/.bin/webdriver-manager start",
    "protractor": "./node_modules/.bin/protractor",
    "start": "npm run wupdate && npm run wstart && npm run protractor"
  }
}
